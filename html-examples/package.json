{
  "name": "html-examples",
  "scripts": {
    "format": "prettier -w examples",
    "build": "npm run postbuild",
    "postbuild": "./postbuild.sh",
    "deploy": "wrangler deploy"
  },
  "prettier": {
    "singleQuote": true,
    "proseWrap": "always"
  },
  "devDependencies": {
    "prettier": "^2.8.1"
  },
  "dependencies": {
    "serve": "^14.1.2"
  }
}
