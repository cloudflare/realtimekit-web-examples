{
  "name": "realtimekit-web-examples-monorepo",
  "private": true,
  "scripts": {
    "prepare": "pnpm --filter demo-app prepare",
    "start": "pnpm --filter demo-app dev",
    "install-all": "pnpm -r install &&",
    "preinstall": "npx only-allow pnpm"
  },
  "devDependencies": {
    "pnpm": "^7.0.0"
  }
}
